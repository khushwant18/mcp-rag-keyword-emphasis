# .env.example
# ChromaDB settings
CHROMA_PERSIST_DIR=./chroma_db
CHROMA_COLLECTION_NAME=web_documents

# Embedding model
EMBEDDING_MODEL=nomic-ai/nomic-embed-text-v1

# Crawling defaults
MAX_CRAWL_DEPTH=2
MAX_PAGES=10
CHUNK_SIZE=1000

# Optional: API keys (if needed in future)
# OPENAI_API_KEY=your-api-key-here